export default function Loading() {
  return (
    <div className="p-6 space-y-4">
      <div className="flex items-center justify-between gap-4">
        <div className="h-8 w-40 rounded bg-gray-200 animate-pulse dark:bg-gray-800" />
        <div className="h-10 w-72 rounded bg-gray-200 animate-pulse dark:bg-gray-800" />
      </div>

      <div className="rounded-xl border overflow-hidden">
        <div className="grid grid-cols-8 gap-4 p-4 bg-gray-50 dark:bg-gray-900">
          {Array.from({ length: 8 }).map((_, i) => (
            <div key={i} className="h-4 rounded bg-gray-200 animate-pulse dark:bg-gray-800" />
          ))}
        </div>

        {Array.from({ length: 10 }).map((_, r) => (
          <div key={r} className="grid grid-cols-8 gap-4 p-4 border-t">
            {Array.from({ length: 8 }).map((_, c) => (
              <div key={c} className="h-4 rounded bg-gray-200 animate-pulse dark:bg-gray-800" />
            ))}
          </div>
        ))}
      </div>
    </div>
  );
}
